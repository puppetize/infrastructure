# vagrant state files
/boxes/*/.vagrant
# log files for automatically booted boxes
/boxes/*/up.log
# downloaded ISO images for veewee (too big)
/boxes/base/iso/
# exported base boxes and base boxes generated by veewee
/boxes/*/*.box
# site-specific Hiara data (except common.yaml)
/data/*.yaml
# puppet configuration and runtime data
/puppet.conf
/ssl/
